# Copy to .env. The only thing that changes between "local Spark" and "remote cluster"
# is this one line — that's the demo.
#
# Prefer 127.0.0.1 over localhost on macOS (avoids IPv6 localhost quirks).
SPARK_REMOTE=sc://127.0.0.1:15099
